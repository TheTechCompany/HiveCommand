schema: http://localhost:9010/graphql
generates:
  types.ts:
    config: 
      withHooks: true
    plugins:
      - typescript
      - typescript-operations
      - typescript-resolvers
      - typescript-react-apollo