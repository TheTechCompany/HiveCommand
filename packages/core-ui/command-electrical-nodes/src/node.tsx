import { CanvasNode, BoxNode, TextNode, ElectricalSymbol } from './nodes';

export const nodeTypes = {
    electricalSymbol: ElectricalSymbol,
    canvasNode: CanvasNode,

    box: BoxNode,
    text: TextNode,
}

