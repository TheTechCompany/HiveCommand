import React from 'react';
import {render} from '@testing-library/react'
import { HMICanvas } from './HMICanvas'

describe('HMI Canvas', () => {
    it('showWindow updates when state does', () => {
        const {getByLabelText} = render(<HMICanvas id="" />);

        

    })
})